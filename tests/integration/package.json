{
  "name": "ttt-integration-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:stress": "for i in $(seq 1 10); do echo \"--- Run $i/10 ---\" && vitest run || exit 1; done"
  },
  "devDependencies": {
    "tui-use": "0.1.20",
    "vitest": "^4.1.7"
  }
}
